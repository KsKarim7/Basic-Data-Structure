#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define newline "\n"

int main()
{

    // ll n, q;
    // cin >> n >> q;
    // vector<ll> vec(n, 0);
    // vec.push_back(0);

    // for (ll i = 1; i <= n; i++)
    // {
    //     ll x;
    //     cin >> x;
    //     vec[i] = x + vec[i - 1];
    // }

    // for (ll i = 1; i <= q; i++)
    // {
    //     ll l, r;
    //     cin >> l >> r;
    //     cout << (vec[r] - vec[l - 1]) << newline;
    // }
    cout << 5 / 2;

    return 0;
}
